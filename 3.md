# Scaling and Elasticity in Cloud Computing

## Overview

**Elasticity** is the capability to automatically adjust compute resources to match demand.

It covers two dimensions of change:

| Dimension | Direction | Meaning |
|---|---|---|
| Per-instance resources | Scale up / Scale down | More or fewer resources *within* a single instance |
| Instance count | Scale out / Scale in | More or fewer instances serving traffic |

**Why it matters** — effective elasticity improves:

- **Performance** — capacity grows before users feel the slowdown
- **Cost efficiency** — you stop paying for capacity you aren't using
- **Resilience** — load is spread across more (or healthier) resources

---

## Scaling Types

### 1. Vertical Scaling (Scale Up / Down)

**Definition:** Adjusting the resources of a *single* instance — CPU, RAM, or instance size/type.

| Term | Meaning |
|---|---|
| Scale up | Add more resources (e.g., `t3.medium` → `t3.xlarge`) |
| Scale down | Reduce resources |

**Key characteristics:**

- ⚠️ Typically incurs **downtime** — the instance must be stopped/resized or replaced.
- 🚧 Limited by the **largest available instance type** — you eventually hit a ceiling.

**Analogy:** Like replacing a small delivery van with a bigger truck — one vehicle, more capacity, but it has to go off the road while you swap it.

# AWS EC2 Instance Types and Use Cases

Amazon EC2 instance types are grouped into families based on the workload they are designed to handle.

## EC2 Instance Type Families

| Instance Family | Optimized For | Key Characteristics | Common Use Cases | Examples |
|---|---|---|---|---|
| **General Purpose - T** | Burstable workloads | Low baseline CPU with ability to burst | Small websites, development, testing, microservices | `t3`, `t3a`, `t4g` |
| **General Purpose - M** | Balanced workloads | Balanced CPU, memory, and networking | Web servers, application servers, enterprise applications | `m5`, `m6i`, `m7i`, `m7g` |
| **General Purpose - A** | ARM workloads | AWS Graviton processors | Web servers, microservices, containers | `a1`, `a2`, `a3` |
| **Compute Optimized - C** | CPU-intensive workloads | High CPU-to-memory ratio | Batch processing, gaming, scientific computing | `c5`, `c6i`, `c7i`, `c7g` |
| **Memory Optimized - R** | Memory-intensive workloads | High memory-to-CPU ratio | Redis, Memcached, databases, analytics | `r5`, `r6i`, `r7i`, `r7g` |
| **Memory Optimized - X** | Very large memory | Extremely high RAM capacity | SAP HANA, large databases, in-memory analytics | `x1`, `x2idn`, `x2iedn` |
| **Memory Optimized - U** | Ultra-large memory | Hundreds of GB to multiple TB RAM | Large SAP HANA and in-memory databases | `u-6tb1`, `u-9tb1`, `u-12tb1` |
| **Storage Optimized - I** | High-performance storage | High IOPS and low-latency NVMe SSD | NoSQL databases, OpenSearch, data processing | `i3`, `i4i`, `i7i` |
| **Storage Optimized - D** | Large storage capacity | High-capacity local HDD | Hadoop, distributed file systems, data lakes | `d2`, `d3`, `d3en` |
| **Storage Optimized - H** | High disk throughput | High-density HDD storage | Big data, Hadoop, distributed file systems | `h1` |
| **Accelerated Computing - G** | Graphics and ML inference | NVIDIA GPUs | Video processing, graphics, ML inference | `g4dn`, `g5`, `g6` |
| **Accelerated Computing - P** | GPU computing | NVIDIA GPUs | ML training, deep learning, HPC | `p3`, `p4`, `p5` |
| **Accelerated Computing - Inf** | ML inference | AWS Inferentia accelerators | Machine learning inference | `inf1`, `inf2` |
| **Accelerated Computing - Trn** | ML training | AWS Trainium accelerators | Deep-learning model training | `trn1`, `trn2` |
| **High Performance Computing - HPC** | HPC workloads | High CPU and network performance | Scientific simulations, engineering, genomics | `hpc6a`, `hpc7a` |
| **Mac** | macOS workloads | Dedicated Apple Mac hardware | iOS/macOS development and CI/CD | `mac1`, `mac2` |

---

## Quick Selection Guide

| Requirement | Recommended Family |
|---|---|
| Small website | **T** |
| Development / Testing | **T** |
| General production application | **M** |
| ARM-compatible application | **A / Graviton** |
| CPU-heavy application | **C** |
| Redis / Memcached | **R** |
| In-memory database | **R / X / U** |
| Large database | **R / X / U** |
| High IOPS storage | **I** |
| Large local storage | **D / H** |
| Machine Learning Training | **P / Trn** |
| Machine Learning Inference | **G / Inf** |
| Graphics / Video Processing | **G** |
| Scientific Computing | **HPC** |
| iOS/macOS Development | **Mac** |

---

## 1. General Purpose - T

### Optimized For

Burstable workloads.

### Characteristics

- Low baseline CPU usage
- Can burst CPU when required
- Cost-effective for variable workloads
- Uses CPU credits

### Common Use Cases

- Development servers
- Testing environments
- Small websites
- Blogs
- Small APIs
- Low-traffic applications

### Examples

```text
t3
t3a
t4g


### 2. Horizontal Scaling (Scale Out / In)

**Definition:** Adjusting the *number* of instances serving traffic.

| Term | Meaning |
|---|---|
| Scale out | Add instances |
| Scale in | Remove instances |

**Key characteristics:**

- ✅ Can happen **without downtime** — when paired with a load balancer and health checks.
- 🎯 Best suited for **stateless** services, or services with externally managed state (e.g., session data in Redis, not on the instance itself).

**Analogy:** Like adding more delivery vans to the fleet instead of building a bigger one — trucks already on the road keep running while new ones join in.

---

## Vertical vs. Horizontal — Quick Comparison

| Aspect | Vertical Scaling | Horizontal Scaling |
|---|---|---|
| What changes | Size of one instance | Number of instances |
| Downtime | Usually yes | No, with LB + health checks |
| Ceiling | Limited by max instance size | Practically limitless |
| State handling | Simpler (single instance) | Needs stateless design or external state store |
| Typical tooling | Resize instance / change instance type | Auto Scaling Groups, load balancers |

---

## AWS Auto Scaling Groups

- Use **Auto Scaling Groups (ASG)** with Launch Templates or Launch Configurations referencing a pre-baked AMI.
- Attach to an **Application/Network Load Balancer** with health checks.
- Configure scaling policies:
  * Target tracking
  * Step scaling
  * Scheduled scaling
- Options for instance protection and draining.

- create ASG watching class room recording

![preview](https://i0.wp.com/directdevops.blog/wp-content/uploads/2026/03/Screenshot-2026-03-08-092435.png?resize=300%2C144&ssl=1)

```sh
#!/bin/bash

while true; do
  stress --cpu 8 --io 4 --vm 2 --vm-bytes 128M --timeout 60s -v
  sleep 30s
done
```



